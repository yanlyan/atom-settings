#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../travis-ci/bin/travis-ci.js" "$@"
  ret=$?
else 
  node  "$basedir/../travis-ci/bin/travis-ci.js" "$@"
  ret=$?
fi
exit $ret
